# Project-SOC
 this a is a bash script designed to allow a user to choose between 3 types of attacks (Nmap, masscan, medusa) on a host machine using functions. The type for attack executed will be saved in a log file in var/log for analysis and documentation
